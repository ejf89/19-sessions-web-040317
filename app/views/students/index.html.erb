<h1>All Students</h1>

<ul>
  <% @students.each do |student| %>
    <li><%= link_to student.name, student %></li>
  <% end %>
</ul>

<% if logged_in? %>
  <%= link_to "Create a Student", new_student_path %>
<% end %>
